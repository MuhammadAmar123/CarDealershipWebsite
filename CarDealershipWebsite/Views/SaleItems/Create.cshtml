@model CarDealershipWebsite.Models.SaleItem

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>SaleItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SaleId" class="control-label"></label>
                <select asp-for="SaleId" class ="form-control" asp-items="ViewBag.SaleId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CarsStockId" class="control-label"></label>
                <select asp-for="CarsStockId" class ="form-control" asp-items="ViewBag.CarsStockId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
